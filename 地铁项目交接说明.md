# 广州地铁生活便利度地图 · 项目交接说明（仅地铁项目）

> 在新对话开头粘贴本文件即可继续开发，无需本对话的历史。

## 1. 项目是什么
用高德官方 POI 数据给广州 333 个地铁站算"生活便利指数"，生成**自包含交互地图**（点站、查排名、切底图、手机抽屉适配）。已上线：`https://toolwu.github.io/gz-metro-map/`。

## 2. 关键路径
- 源码+数据：`C:\Users\Administrator\Desktop\知乎未答\gz_metro_map_codex\`
- 成品：`C:\Users\Administrator\Desktop\知乎未答\gz_metro_map_codex\output\广州地铁生活便利地图.html`
- 本工作区副本：`C:\Users\Administrator\Desktop\地铁便利度地图作品集\`（也是 git 仓库）
- 在线仓库：`toolwu/gz-metro-map`（public，推 main 即自动重建，1~2 分钟）

## 3. 架构（src/）
- `stations.py`：高德地铁公开接口抓站/线，坐标 GCJ→WGS
- `amap_poi.py`：高德 POI 采集（周边搜索、半径300m、count 600封顶自动降250m）、缓存
- `gcj_convert.py`：高德官方坐标转换 API（WGS→GCJ）、缓存
- `osm_poi.py`：OSM pbf 解析（备用数据源）
- `index.py`：便利指数算法（百分位加权 + 大型商场质量加权）
- `build_map.py`：**核心**，生成交互地图（含全部 JS 交互/底图/抽屉/缩放）
- `inline_assets.py`：把 CDN 资源内嵌成单文件（必须调用）
- `summary.py`：TOP 排名摘要
- `data/`：index.json（333站+指数）、poi_stats.json、stations.json、amap_poi_cache.json、gcj_cache.json 等

## 4. 便利指数算法
`指数 = 餐饮25% + 购物20% + 生活20% + 交通15% + 医疗10% + 教育10%`；购物 = 数量70% + 大型商场30%；各类按全市百分位归一化；每站 300m 圈内高德 POI 计数。

## 5. 重新生成地图
```bat
cd C:\Users\Administrator\Desktop\知乎未答\gz_metro_map_codex
python src\build_map.py
```

## 6. 踩过的坑（改代码时务必保留这些处理，别破坏）
1. folium 引用外部 CDN 的 leaflet → 必须调 `inline_assets.inline_file` 内嵌，否则白屏
2. 地图变量名随机（`map_xxx`）→ JS 里用 `m.get_name()`，别写死 `map`
3. 注入的 JS **不要带 `<script>` 包裹标签**（会嵌套导致地图初始化代码变文本）
4. 地图初始化后的 JS 在 `DOMContentLoaded` 里跑（`window.load` 可能被瓦片拖晚）
5. 底图图层**没有 `options.name`** → 用 `layer._baseName`（按添加顺序打标记）；`baselayerchange` 事件用 `e.name` 判断
6. **高德用 GCJ-02**：切高德底图时矢量坐标用官方 GCJ（gcj_convert 预存 `_gcj`），否则偏移
7. 高德 POI `count` 封顶 600 → 半径用 300m；封顶自动降 250m
8. **f-string 里写 JS，花括号必须 `{{ }}`**
9. **热力层是全局 `heat_xxx`**，`map.eachLayer` 遍历不到 → 按全局变量名 `heat_` 抓取，存 `_wgs/_gcj`
10. folium 不给标记传 `options.title` → 站名用 `_name`（按 `MARKER_NAMES` 顺序映射），筛选也用 `_name`
11. 高德 key 在 `secrets.json`（已 gitignore），**绝不提交/外传**

## 7. 已有功能
- 底图 ESRI/Carto/OSM/高德 可切换，坐标 WGS↔GCJ 自适应无偏移
- 站点圆点按指数着色（≥75绿 / 50~74黄 / <50红），**随缩放自适应大小**
- **放大 zoom≥14 显示站名标签**（`.st-label`）
- 筛选：指数阈值、只看换乘站、线路**全选/全不选**
- 查询：按站名/排名查 → 选中弹出详情；TOP20 榜单一键跳转
- POI 密度热力层；**移动端抽屉**（默认收起，点 ☰ 滑出，✕ 收起）
- 底图失败自动切换兜底；自包含单文件可离线打开

## 8. 安全合规
全公开数据（地铁站/POI计数/指数），无隐私、无机密，个人作品集合法；勿商用、勿公开 key。

## 9. 可选下一步
- 综合宜居指数（叠加租金）
- 导出完整 333 名榜单
- 效果图重截 / README 更新
